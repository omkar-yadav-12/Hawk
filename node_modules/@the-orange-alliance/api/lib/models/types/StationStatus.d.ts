declare enum StationStatus {
    Unknown = -1,
    Surrogate = 0,
    Normal = 1,
    SitOut = 2
}
export declare const numberToStationStatus: (stationStatus: number) => StationStatus;
export declare const stationStatusToNumber: (station: StationStatus) => number;
export default StationStatus;
